# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RLLastLineTextView' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for RLLastLineTextView
  pod 'Masonry'
  target 'RLLastLineTextViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RLLastLineTextViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
